name: multiplayer-games

services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    image: multiplayer-games-web
    ports:
      - "8080:80"
    restart: unless-stopped

# Future services, added as each component is built:
#
#   game-server:                      # Rust axum/tokio authoritative WebSocket server
#     build:
#       context: .
#       dockerfile: crates/server/Dockerfile
#     ports:
#       - "8081:8081"
#     environment:
#       DATABASE_URL: postgres://postgres:postgres@db:5432/app
#     depends_on:
#       - db
#     restart: unless-stopped
#
#   db:                               # PostgreSQL (stats / leaderboards)
#     image: postgres:17-alpine
#     environment:
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: app
#     volumes:
#       - db-data:/var/lib/postgresql/data
#     restart: unless-stopped
#
#   reverse-proxy:                    # Caddy: TLS + routes static/web, /api, and wss
#     image: caddy:2-alpine
#     ports:
#       - "80:80"
#       - "443:443"
#     volumes:
#       - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
#     depends_on:
#       - web
#       - game-server
#     restart: unless-stopped
#
# volumes:
#   db-data:
