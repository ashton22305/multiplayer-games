[package]
name = "engine"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
macroquad = "0.4"
protocol = { path = "../protocol" }
serde_json = "1"
postcard = { version = "1", default-features = false, features = ["alloc"] }
