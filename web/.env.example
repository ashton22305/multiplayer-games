# Base URL of the separately-deployed games Cloudflare Pages project.
# Set this in your GitHub environment (dev / prod) or locally when testing cross-origin.
# When unset, iframes are loaded same-origin from /games/<game>/ (local dev + Docker).
# VITE_GAMES_BASE_URL=https://games.example.com
