name: Deploy - prod

on:
  push:
    branches: [prod]

jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  deploy:
    needs: ci
    if: ${{ vars.DEPLOY_ENABLED == 'true' }}
    uses: ./.github/workflows/deploy-web.yml
    with:
      environment: prod
    secrets: inherit
